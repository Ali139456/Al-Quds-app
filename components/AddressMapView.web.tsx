import type { AddressMapViewProps } from './AddressMapView.types';

/** Maps are not used on web — parent shows OSM embed or placeholder. */
export default function AddressMapView(_props: AddressMapViewProps) {
  return null;
}
